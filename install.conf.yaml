- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.bash_profile:
    ~/.bashrc:
    ~/.hammerspoon:
    ~/.oh-my-zsh:
    ~/.ssh:
    ~/.tmux.conf:
    ~/.vim:  
    ~/.vimrc:
    ~/.zshrc: 

- shell:
  - git submodule sync --recursive
